DATABASE_URL="postgresql://postgres:password@localhost:5432/password_manager"
JWT_SECRET="<random 64-char hex — run: node -e \"console.log(require('crypto').randomBytes(64).toString('hex'))\">"
CREDENTIALS_SECRET="<random 64-char hex — run: node -e \"console.log(require('crypto').randomBytes(64).toString('hex'))\">"
